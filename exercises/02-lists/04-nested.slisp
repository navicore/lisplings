; === Nested Lists ===
;
; Lists can contain other lists:
;   (list (list 'a 'b) (list 'c 'd))
;
; Use car and cdr to navigate nested structures.
; (car (cdr lst)) gets the second element.
;
; Your task: Extract the symbol 'b from the nested list.

; I AM NOT DONE

(define nested (list (list 'a 'b) (list 'c 'd)))
(define result 0)

;; ===== Tests - do not edit below this line =====
(define (assert-eq actual expected msg)
  (if (equal? actual expected)
      #t
      (begin (print (list 'FAIL msg 'expected expected 'got actual))
             (exit 1))))

(assert-eq result 'b "result should be the symbol b")
(exit 0)
